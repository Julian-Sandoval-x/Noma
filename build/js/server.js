import express from"express";import path from"path";import{fileURLToPath}from"url";const{static:serveStatic}=express,app=express(),port=process.env.PORT||8080,__filename=fileURLToPath(import.meta.url),__dirname=path.dirname(__filename);app.use(express.static(path.join(__dirname,"../.."))),app.get("/",((e,r)=>{r.sendFile(path.join(__dirname,"..","index.html"))})),app.listen(port,(()=>{console.log(`Servidor escuchando en el puerto ${port}, dir  ../../${__dirname}`)}));